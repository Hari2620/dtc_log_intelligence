# "mock" needs no key: a naive lookup-baseline "diagnosis" used as the honest
# floor an LLM has to beat. Set to "anthropic" to use the real Messages API.
LLM_PROVIDER=mock
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-latest
